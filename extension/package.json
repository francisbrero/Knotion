{
  "name": "knotion-extension",
  "version": "0.1.0",
  "description": "KNotion browser extension for highlighting and commenting on web pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "nodemon --watch src --watch manifest.json --watch popup.html --watch popup.js --exec \"node build.js\""
  },
  "dependencies": {
    "mark.js": "^8.11.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "nodemon": "^3.0.2"
  }
} 